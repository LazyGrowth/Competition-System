services:
  - type: web
    name: competition-system-backend
    env: node
    plan: free
    buildCommand: |
      cd "Competition System/backend"
      npm install
      npx prisma generate
      npm run build
    startCommand: |
      cd "Competition System/backend"
      npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: PORT
        value: 10000
